{
  "name": "smart-trader-hub-desktop",
  "version": "0.1.0",
  "description": "Smart Trader Hub - Windows Desktop Application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "echo 'Build script not yet implemented. Use electron-builder or similar.'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "smart",
    "trader",
    "hub",
    "desktop",
    "electron"
  ],
  "author": "Smart Trader Hub Team",
  "license": "UNLICENSED",
  "devDependencies": {
    "electron": "^27.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
    // Add other dependencies as needed, e.g., for UI framework if not plain HTML/JS
  }
}
